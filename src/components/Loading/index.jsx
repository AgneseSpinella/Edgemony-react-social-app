import styles from "./Loading.module.scss"

const Loading = () => {
    return (
        <div className={styles.loading}>
            Waiting for Loading
        </div>
    )
}

export default Loading